


xóa file .class trong folder bằng các loại  command line:
	+ đối với power shell: Get-ChildItem -Path . -Filter "*.class" | ForEach-Object { Remove-Item $_.FullName }
	+ đối với command fromt: del /s /q *.class
	+ đối với terminal(bash): find . -name "*.class" -type f -exec rm -f {} \;

 	
xóa một folder với toàn bộ nọi dung bên trong : rm -rf <đường dẫn>

cargo test --features grapheme


# cần lưu ý về bộ gõ unicode và unicode tổ hợp
print('xin chào' == 'xin chào') => false 
   unicode tổ hợp  & unicode


biên dịch file c++: g++ name.cpp
chạy file out:
	command frompt: a.exe out
	power shell: ./a out


